/**
 * Placeholder test — replace with component tests as the app grows.
 * Confirms Vitest + jsdom + testing-library are wired correctly.
 */
describe('Frontend test suite', () => {
  it('environment is jsdom', () => {
    expect(typeof document).toBe('object');
  });

  it('placeholder — passes until real component tests are added', () => {
    expect(true).toBe(true);
  });
});
