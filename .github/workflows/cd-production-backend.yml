name: CD — Production Backend

on:
  push:
    branches:
      - main
    paths:
      - 'expensy_backend/**'
      - 'k8s/apps/backend/production/**'
      - 'k8s/apps/backend/base/**'

permissions:
  id-token: write
  contents: write

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set image tag
        id: tag
        run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ACR
        uses: docker/login-action@v3
        with:
          registry: acr2poiclyh4krxy.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./expensy_backend
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: NODE_OPTIONS=--jitless
          tags: |
            acr2poiclyh4krxy.azurecr.io/expensy-backend:${{ steps.tag.outputs.sha }}
            acr2poiclyh4krxy.azurecr.io/expensy-backend:latest

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: acr2poiclyh4krxy.azurecr.io/expensy-backend:${{ steps.tag.outputs.sha }}
          format: table
          exit-code: '0'
          severity: CRITICAL,HIGH

      - name: Install Kustomize
        uses: imranismail/setup-kustomize@v2

      - name: Update production kustomization
        working-directory: k8s/apps/backend/production
        run: |
          kustomize edit set image \
            acr2poiclyh4krxy.azurecr.io/expensy-backend=acr2poiclyh4krxy.azurecr.io/expensy-backend:${{ steps.tag.outputs.sha }}

      - name: Commit and push updated kustomization
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/apps/backend/production/kustomization.yaml
          git diff-index --quiet HEAD || \
            git commit -m "chore: update backend production image to ${{ steps.tag.outputs.sha }} [skip ci]"
          git pull --rebase origin main
          git push origin main
